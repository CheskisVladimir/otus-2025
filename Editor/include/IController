#pragma once
#include "std_includes.h"

class IMessage;
class IDocument;

class IController
{
public:
    IController() = default;
    virtual ~IController() = default;

    virtual bool process_message(const IMessage &message, IDocument &document) = 0;
};